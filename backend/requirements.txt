# Ask the Eagle - backend runtime dependencies (curated from the full freeze).
# These are the DIRECT packages main.py imports; pip resolves the rest (groq
# SDK, pydantic, numpy, etc.) automatically. Embeddings run on Pinecone's
# hosted model, so torch / transformers / sentence-transformers are NOT needed.
# Versions pinned to the ones you tested with.

# --- web server ---
fastapi==0.136.3
uvicorn==0.48.0
python-multipart==0.0.29     # required by FastAPI for the /transcribe file upload
python-dotenv==1.2.2

# --- HTTP (ElevenLabs /speak + Groq Whisper /transcribe calls) ---
httpx==0.28.1

# --- RAG stack ---
pinecone==7.3.0
llama-index-core==0.14.22
llama-index-vector-stores-pinecone==0.8.0
llama-index-llms-groq==0.5.0
llama-index-postprocessor-cohere-rerank>=0.6.0   # Cohere cross-encoder reranker
cohere>=5.11.0                                    # rerank API client (needs COHERE_API_KEY in .env)

# --- voice + language ---
edge-tts==7.2.8              # free TTS fallback when ElevenLabs quota runs out
langdetect==1.0.9            # question-language detection (UI + answer pinning)

# --- ingestion only (not needed by the live API; uncomment if you run ingest.py
#     on the same machine) ---
# tqdm==4.67.3
